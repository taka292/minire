<div class="bg-gray-100 min-h-screen py-8 px-4">
  <div class="max-w-4xl mx-auto">
    <h2 class="text-2xl font-semibold text-gray-800 mb-6 text-center">レビュー編集</h2>
    <%= form_with model: @review, local: true do |f| %>
      <%= render "reviews/form", form: f, button_label: "更新する" %>
    <% end %>
  </div>
</div>
