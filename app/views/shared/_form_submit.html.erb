<!-- 投稿ボタン-->
<div class="form-control mt-6">
  <%= form.submit button_label, class: "btn bg-customBlue text-white border-0 hover:bg-blue-500 w-full py-3 text-lg", data: { action: "click->loading#show" } %>
  <!-- 下書き保存ボタン -->
  <%= form.submit "下書き保存", name: "draft",
        class: "btn bg-gray-400 text-white border-0 hover:bg-gray-500 w-full py-3 text-lg mt-4", data: { action: "click->loading#show" } %>
</div>
